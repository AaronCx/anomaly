# No environment variables required!
# The app runs with zero configuration.
#
# Optional:
# OPENAI_API_KEY is entered by the user in the browser UI, not set as an env var.
# GITHUB_TOKEN is entered by the user in the browser UI for higher rate limits.
