# OpenAI (for AI annotations — optional)
OPENAI_API_KEY=sk-...

# GitHub (optional — for higher rate limits on public repos)
GITHUB_TOKEN=ghp_...

# Backend URL (auto-detected in production)
NEXT_PUBLIC_API_URL=http://localhost:3000
